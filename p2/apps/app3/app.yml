---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress_app3
  # annotations:
  #   kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  -host: app3.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: service_app3
            port:
              number: 80

---
apiVersion: v1
kind: Service
metadata:
  name: service_app3
spec:
  ports:
    - name: http
      port: 80
      # protocol: TCP
  selector:
    app:  app3

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3
spec:
  selector:
    matchLabels:
      app: app3
  # replicas: 3
  template:
    metadata:
      labels:
        app: app3
    spec:
      containers:
      - name: app3
        image: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - name: app3_volume
          mountPath: /usr/share/nginx/html
      volumes:
      - name: app3_volume
        configMap:
          name: app3_html